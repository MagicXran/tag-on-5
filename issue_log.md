# Issue Log

## 2026-08-22 SQLite v2 与第三方推送改造

### 背景

原系统将清洗后的三类数据保存到旧库并转换为OA专用表单结构。收支汇总重复写入流水，零流水账户依赖占位记录，外部接口ID与业务数据耦合。

### 调整

- 新建 `data/rms_v2.db`，保留旧 `data/rms.db`。
- 合同、到账、收支汇总、收支流水分为四张表。
- 三类原业务唯一键保持不变；汇总表增加期间唯一键。
- 零流水账户只保存汇总。
- 删除OA和MySQL运行路径，改为统一第三方批量POST。
- 请求失败固定间隔3秒重试，最多4次请求；最终失败不回滚SQLite。

### 防回归

- 最新合同样本：24条有效，拆分后27条。
- 最新到账样本第一工作表：560条原始、558条有效。
- 最新收支目录：474个文件、474条汇总、688条流水、0个解析失败。
- 自动测试校验四张表的唯一键和第三方重试使用相同 `requestId`。

## 2026-08-22 数据边界与接口校验加固

### 问题

- 非空非法日期会原样进入SQLite和第三方请求。
- 收支流水没有强制校验经费号及所属期间。
- Python布尔值可能被当作接口整数响应接受。
- SQLite相对路径会随程序启动目录变化。
- 数据库管理窗口清空表时缺少 `DATABASE_TYPE` 导入。

### 修复

- 非空日期解析失败时明确报错，不再发送非标准日期。
- 保存收支文件前校验流水经费号一致且交易日期位于汇总期间内。
- 严格要求 `code`、`successCount`、`failedCount` 为非布尔整数。
- SQLite路径固定为项目目录下的 `data/rms_v2.db`。
- 第三方请求显式发送 `application/json; charset=utf-8`。
- 增加真实本地HTTP批量、重试、请求体一致性回归测试。
